name: Send GET request to macio.dev

on:
  schedule:
    - cron: "*/8 * * * *"  # Cron syntax for every 10 minutes
  workflow_dispatch:  # Allows manual triggering of the workflow

jobs:
  send-request:
    runs-on: ubuntu-latest
    steps:
      - name: Send GET request to macio.dev
        run: |
          curl -I https://macio.dev
